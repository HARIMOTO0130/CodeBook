# 教材提供者端功能开发文档

## 1. 核心定位

### 1.1 平台定位
教材提供者端是数字教材平台的内容创作与管理核心,为教材作者、出版社和教育机构提供全方位的教材资源管理、内容创作、版本控制和协作审核能力。教材提供者端通过专业化的编辑工具和智能化的管理系统,帮助内容创作者高效地生产、管理和发布优质教材资源。

### 1.2 核心价值
- **内容创作效率**：提供强大的在线编辑器,支持多种内容格式,大幅提升创作效率
- **版本管理**：完善的版本控制系统,支持版本历史、分支管理和回滚操作
- **协作审核**：多人协作编辑、审核流程管理,确保内容质量
- **智能辅助**：AI辅助写作、智能排版、自动生成练习题等智能化功能
- **多格式支持**：支持多种文件格式导入导出,满足不同场景需求
- **数据分析**：内容使用数据分析,帮助优化教材内容

### 1.3 用户画像
- **主要用户**：教材作者、出版社编辑、教育机构内容创作者、课程开发者
- **次要用户**：审核人员、内容管理员、教学设计师
- **使用场景**：教材创作、内容更新、版本管理、协作编辑、审核发布

## 2. 功能模块总览

### 2.1 功能模块架构
```
教材提供者端功能模块
├── 1. 书籍管理（核心）
│   ├── 书籍创建与上传
│   ├── 书籍列表与搜索
│   ├── 书籍详情与编辑
│   └── 书籍删除与归档
├── 2. 内容编辑（核心）
│   ├── 章节编辑器
│   ├── 富文本编辑
│   ├── 代码编辑
│   └── 多媒体资源管理
├── 3. 版本管理（核心）
│   ├── 版本创建
│   ├── 版本历史
│   ├── 分支管理
│   ├── 版本对比
│   └── 版本回滚
├── 4. 分类与标签（核心）
│   ├── 分类体系管理
│   ├── 标签管理
│   └── 智能推荐
├── 5. 状态管理（核心）
│   ├── 草稿管理
│   ├── 审核流程
│   ├── 发布管理
│   └── 下架管理
├── 6. 协作管理（暂不做）
│   ├── 多人协作
│   ├── 评论与反馈
│   ├── 审核流程
│   └── 权限管理


### 2.2 功能优先级
- **P0（必须实现）**：书籍管理、内容编辑、版本管理、分类与标签、状态管理
- **P1（重要功能）**：资源管理、协作管理
- **P2（增强功能）**：数据分析、AI辅助功能

## 3. 核心功能模块详细设计

### 3.1 书籍管理

#### 3.1.1 书籍创建与上传
**功能描述**：创建新的教材或上传已有教材文件

**核心功能**：
- **创建空白书籍**
  - 设置书籍基本信息（标题、副标题、作者、ISBN）
  - 选择书籍分类和标签
  - 设置书籍封面
  - 设置书籍描述和简介
  - 选择编程语言（技术类书籍）

- **上传书籍文件**
  - 支持PDF格式上传
  - 支持Word文档上传（.docx）
  - 支持Markdown文件上传（.md）
  - 支持EPUB格式上传
  - 自动解析文件内容并转换为平台格式
  - 保留原始文件结构

- **导入外部资源**
  - 从GitHub导入代码仓库
  - 从其他平台导入教材
  - 批量导入章节内容
  - 导入练习题库

- **书籍模板**
  - 使用预设书籍模板
  - 自定义书籍模板
  - 保存为模板供后续使用

**数据模型**：
```python
Book:
- id: 主键
- title: 书名
- subtitle: 副标题
- author: 作者
- isbn: ISBN编号
- cover: 封面图片
- description: 描述
- introduction: 简介
- language: 编程语言（技术类书籍）
- category: 分类（外键关联Category）
- tags: 标签（多对多关联Tag）
- owner: 创建者（外键关联User）
- collaborators: 协作者（多对多关联User）
- status: 状态（draft/review/published/archived）
- current_version: 当前版本号
- total_chapters: 章节数量
- word_count: 字数统计
- created_at: 创建时间
- updated_at: 更新时间
- published_at: 发布时间
```

**API接口**：
- `POST /api/provider/books/` - 创建书籍
- `POST /api/provider/books/upload/` - 上传书籍文件
- `POST /api/provider/books/import/github/` - 从GitHub导入
- `POST /api/provider/books/import/external/` - 从外部平台导入
- `GET /api/provider/books/templates/` - 获取书籍模板
- `POST /api/provider/books/templates/` - 创建书籍模板

**前端页面**：
- `BookCreateView.vue` - 书籍创建页面
- `BookUploadView.vue` - 文件上传页面
- `BookImportView.vue` - 外部导入页面
- `BookTemplateSelector.vue` - 模板选择组件

#### 3.1.2 书籍列表与搜索
**功能描述**：查看和管理所有书籍

**核心功能**：
- **书籍列表**
  - 显示所有书籍
  - 显示书籍封面、标题、作者、状态
  - 显示创建时间、更新时间
  - 显示版本号
  - 显示章节数量

- **筛选功能**
  - 按状态筛选（草稿/审核中/已发布/已归档）
  - 按分类筛选
  - 按标签筛选
  - 按作者筛选
  - 按时间范围筛选

- **搜索功能**
  - 按标题搜索
  - 按作者搜索
  - 按ISBN搜索
  - 按关键词搜索
  - 高级搜索（组合条件）

- **排序功能**
  - 按创建时间排序
  - 按更新时间排序
  - 按标题排序
  - 按状态排序

- **批量操作**
  - 批量删除
  - 批量归档
  - 批量发布
  - 批量导出

**API接口**：
- `GET /api/provider/books/` - 获取书籍列表
- `GET /api/provider/books/search/` - 搜索书籍
- `POST /api/provider/books/batch-delete/` - 批量删除
- `POST /api/provider/books/batch-archive/` - 批量归档
- `POST /api/provider/books/batch-publish/` - 批量发布
- `POST /api/provider/books/batch-export/` - 批量导出

**前端页面**：
- `BookListView.vue` - 书籍列表页面
- `BookSearchBar.vue` - 搜索栏组件
- `BookFilterPanel.vue` - 筛选面板组件
- `BookCard.vue` - 书籍卡片组件

#### 3.1.3 书籍详情与编辑
**功能描述**：查看和编辑书籍详细信息

**核心功能**：
- **书籍详情**
  - 显示书籍完整信息
  - 显示章节列表
  - 显示版本历史
  - 显示协作者列表
  - 显示统计数据（学习人数、评价等）

- **基本信息编辑**
  - 编辑标题、副标题、作者
  - 编辑ISBN、分类、标签
  - 更换封面图片
  - 编辑描述和简介
  - 修改编程语言

- **高级设置**
  - 设置访问权限（公开/私有/指定用户）
  - 设置下载权限
  - 设置评论权限
  - 设置审核流程
  - 设置自动保存间隔

- **书籍统计**
  - 学习人数统计
  - 学习时长统计
  - 评价统计
  - 下载次数统计
  - 分享次数统计

**API接口**：
- `GET /api/provider/books/{id}/` - 获取书籍详情
- `PUT /api/provider/books/{id}/` - 更新书籍信息
- `PATCH /api/provider/books/{id}/` - 部分更新书籍信息
- `GET /api/provider/books/{id}/stats/` - 获取书籍统计
- `PUT /api/provider/books/{id}/settings/` - 更新书籍设置

**前端页面**：
- `BookDetailView.vue` - 书籍详情页面
- `BookBasicInfoEditor.vue` - 基本信息编辑器
- `BookAdvancedSettings.vue` - 高级设置面板
- `BookStatsPanel.vue` - 统计面板

#### 3.1.4 书籍删除与归档
**功能描述**：删除或归档书籍

**核心功能**：
- **删除书籍**
  - 软删除（移至回收站）
  - 硬删除（永久删除）
  - 删除确认提示
  - 删除影响提示（关联数据）

- **归档书籍**
  - 归档操作
  - 归档原因记录
  - 归档书籍列表
  - 恢复归档书籍

- **回收站**
  - 查看已删除书籍
  - 恢复已删除书籍
  - 永久删除
  - 清空回收站

**API接口**：
- `DELETE /api/provider/books/{id}/` - 删除书籍
- `POST /api/provider/books/{id}/archive/` - 归档书籍
- `POST /api/provider/books/{id}/restore/` - 恢复书籍
- `GET /api/provider/books/trash/` - 获取回收站书籍
- `POST /api/provider/books/trash/restore/{id}/` - 恢复书籍
- `DELETE /api/provider/books/trash/{id}/` - 永久删除
- `POST /api/provider/books/trash/clear/` - 清空回收站

**前端页面**：
- `BookDeleteConfirmModal.vue` - 删除确认弹窗
- `BookArchiveModal.vue` - 归档弹窗
- `TrashView.vue` - 回收站页面

### 3.2 内容编辑

#### 3.2.1 章节编辑器
**功能描述**：强大的在线章节编辑器

**核心功能**：
- **章节管理**
  - 创建新章节
  - 编辑现有章节
  - 删除章节
  - 章节排序（拖拽排序）
  - 章节复制
  - 章节移动

- **章节类型**
  - 阅读型章节：文本+图片+代码
  - 视频型章节：视频内容
  - 练习型章节：练习题
  - Jupyter型章节：交互式文档
  - 混合型章节：多种内容组合

- **章节属性**
  - 设置章节标题
  - 设置章节类型
  - 设置预计学习时长
  - 设置章节描述
  - 设置章节标签

- **章节预览**
  - 实时预览
  - 预览模式切换
  - 移动端预览
  - 打印预览

**数据模型**：
```python
Chapter:
- id: 主键
- book: 所属书籍（外键关联Book）
- title: 章节标题
- type: 章节类型（reading/video/practice/jupyter/mixed）
- duration: 预计时长(分钟)
- description: 章节描述
- content: 章节内容（Markdown）
- code: 示例代码
- jupyter_content: Jupyter文档内容
- merged_content: 合并内容
- language: 编程语言
- content_type: 内容类型（markdown/jupyter）
- video_url: 视频URL
- video_file: 视频文件
- tags: 标签（多对多关联Tag）
- order: 排序
- version: 版本号
- created_at: 创建时间
- updated_at: 更新时间
- created_by: 创建者（外键关联User）
- updated_by: 更新者（外键关联User）
```

**API接口**：
- `POST /api/provider/books/{book_id}/chapters/` - 创建章节
- `GET /api/provider/books/{book_id}/chapters/` - 获取章节列表
- `GET /api/provider/chapters/{id}/` - 获取章节详情
- `PUT /api/provider/chapters/{id}/` - 更新章节
- `DELETE /api/provider/chapters/{id}/` - 删除章节
- `POST /api/provider/books/{book_id}/chapters/reorder/` - 章节排序
- `POST /api/provider/chapters/{id}/copy/` - 复制章节
- `POST /api/provider/chapters/{id}/move/` - 移动章节
- `GET /api/provider/chapters/{id}/preview/` - 预览章节

**前端页面**：
- `ChapterEditorView.vue` - 章节编辑器页面
- `ChapterListView.vue` - 章节列表页面
- `ChapterTypeSelector.vue` - 章节类型选择器
- `ChapterPreviewModal.vue` - 预览弹窗

#### 3.2.2 富文本编辑
**功能描述**：支持富文本内容的编辑

**核心功能**：
- **文本编辑**
  - Markdown语法支持
  - 实时预览
  - 语法高亮
  - 自动保存

- **格式工具**
  - 标题（H1-H6）
  - 粗体、斜体、下划线
  - 删除线、上标、下标
  - 引用、代码块
  - 列表（有序/无序）
  - 分割线

- **插入功能**
  - 插入图片
  - 插入视频
  - 插入链接
  - 插入表格
  - 插入公式（LaTeX）
  - 插入代码块

- **样式设置**
  - 字体设置
  - 字号设置
  - 颜色设置
  - 对齐方式
  - 行距设置

- **辅助功能**
  - 撤销/重做
  - 查找/替换
  - 全屏模式
  - 导出为PDF
  - 导出为Word

**前端组件**：
- `RichTextEditor.vue` - 富文本编辑器组件
- `MarkdownEditor.vue` - Markdown编辑器组件
- `Toolbar.vue` - 工具栏组件
- `InsertMenu.vue` - 插入菜单组件

#### 3.2.3 代码编辑
**功能描述**：支持代码内容的编辑和展示

**核心功能**：
- **代码编辑**
  - 语法高亮
  - 代码自动补全
  - 代码格式化
  - 代码折叠
  - 多光标编辑

- **语言支持**
  - Python
  - JavaScript
  - Java
  - C/C++
  - Go
  - Rust
  - 其他编程语言

- **代码执行**
  - 在线代码执行
  - 输出结果展示
  - 错误信息显示
  - 执行历史

- **代码模板**
  - 预置代码模板
  - 自定义代码模板
  - 代码片段管理

- **代码分享**
  - 生成代码分享链接
  - 代码嵌入
  - 导出代码

**前端组件**：
- `CodeEditor.vue` - 代码编辑器组件
- `CodeExecutionPanel.vue` - 代码执行面板
- `CodeTemplateSelector.vue` - 代码模板选择器
- `CodeShareModal.vue` - 代码分享弹窗

#### 3.2.4 多媒体资源管理
**功能描述**：管理章节中的多媒体资源

**核心功能**：
- **图片管理**
  - 上传图片
  - 图片裁剪
  - 图片压缩
  - 图片格式转换
  - 图片库管理

- **视频管理**
  - 上传视频
  - 视频转码
  - 视频压缩
  - 视频封面设置
  - 视频库管理

- **音频管理**
  - 上传音频
  - 音频格式转换
  - 音频剪辑
  - 音频库管理

- **文档管理**
  - 上传文档
  - 文档预览
  - 文档转换
  - 文档库管理

- **资源引用**
  - 在内容中引用资源
  - 资源替换
  - 资源删除
  - 资源统计

**数据模型**：
```python
MediaResource:
- id: 主键
- book: 所属书籍（外键关联Book）
- chapter: 所属章节（外键关联Chapter, 可为空）
- type: 资源类型（image/video/audio/document）
- file: 文件
- url: 资源URL
- name: 资源名称
- size: 文件大小
- format: 文件格式
- description: 描述
- created_at: 创建时间
- created_by: 上传者（外键关联User）
```

**API接口**：
- `POST /api/provider/books/{book_id}/resources/upload/` - 上传资源
- `GET /api/provider/books/{book_id}/resources/` - 获取资源列表
- `GET /api/provider/resources/{id}/` - 获取资源详情
- `PUT /api/provider/resources/{id}/` - 更新资源信息
- `DELETE /api/provider/resources/{id}/` - 删除资源
- `POST /api/provider/resources/{id}/crop/` - 裁剪图片
- `POST /api/provider/resources/{id}/convert/` - 转换格式

**前端页面**：
- `ResourceLibraryView.vue` - 资源库页面
- `ImageUploader.vue` - 图片上传器
- `VideoUploader.vue` - 视频上传器
- `ResourcePreviewModal.vue` - 资源预览弹窗

### 3.3 版本管理

#### 3.3.1 版本创建
**功能描述**：创建书籍或章节的新版本

**核心功能**：
- **手动创建版本**
  - 创建书籍版本
  - 创建章节版本
  - 添加版本说明
  - 选择版本类型（major/minor/patch）

- **自动版本控制**
  - 自动保存草稿
  - 定时创建版本
  - 提交时创建版本
  - 发布前创建版本

- **版本标签**
  - 添加版本标签
  - 版本里程碑
  - 版本发布说明

**数据模型**：
```python
BookVersion:
- id: 主键
- book: 所属书籍（外键关联Book）
- version: 版本号（如1.0.0）
- version_type: 版本类型（major/minor/patch）
- description: 版本说明
- changelog: 变更日志
- is_published: 是否已发布
- created_at: 创建时间
- created_by: 创建者（外键关联User）

ChapterVersion:
- id: 主键
- chapter: 所属章节（外键关联Chapter）
- version: 版本号
- content: 章节内容
- changes: 变更说明
- created_at: 创建时间
- created_by: 创建者（外键关联User）
```

**API接口**：
- `POST /api/provider/books/{id}/versions/` - 创建书籍版本
- `POST /api/provider/chapters/{id}/versions/` - 创建章节版本
- `GET /api/provider/books/{id}/versions/` - 获取书籍版本列表
- `GET /api/provider/chapters/{id}/versions/` - 获取章节版本列表
- `POST /api/provider/books/{id}/versions/auto-save/` - 自动保存草稿

**前端页面**：
- `VersionCreateModal.vue` - 版本创建弹窗
- `VersionHistoryPanel.vue` - 版本历史面板

#### 3.3.2 版本历史
**功能描述**：查看书籍和章节的版本历史

**核心功能**：
- **版本列表**
  - 显示所有版本
  - 显示版本号、创建时间、创建者
  - 显示版本说明
  - 显示版本类型

- **版本详情**
  - 查看版本详细信息
  - 查看版本变更内容
  - 查看版本创建者信息
  - 查看版本标签

- **版本筛选**
  - 按时间范围筛选
  - 按创建者筛选
  - 按版本类型筛选
  - 按标签筛选

**API接口**：
- `GET /api/provider/books/{id}/versions/` - 获取书籍版本列表
- `GET /api/provider/chapters/{id}/versions/` - 获取章节版本列表
- `GET /api/provider/books/{id}/versions/{version_id}/` - 获取版本详情
- `GET /api/provider/chapters/{id}/versions/{version_id}/` - 获取章节版本详情

**前端页面**：
- `VersionHistoryView.vue` - 版本历史页面
- `VersionTimeline.vue` - 版本时间轴组件
- `VersionDetailModal.vue` - 版本详情弹窗

#### 3.3.3 分支管理
**功能描述**：创建和管理书籍分支

**核心功能**：
- **创建分支**
  - 从主版本创建分支
  - 从指定版本创建分支
  - 设置分支名称
  - 设置分支说明

- **分支管理**
  - 查看所有分支
  - 切换分支
  - 删除分支
  - 合并分支

- **分支对比**
  - 对比分支差异
  - 查看分支变更
  - 分支冲突解决

**数据模型**：
```python
BookBranch:
- id: 主键
- book: 所属书籍（外键关联Book）
- name: 分支名称
- description: 分支说明
- parent_branch: 父分支（外键关联自身）
- base_version: 基础版本（外键关联BookVersion）
- is_active: 是否活跃
- created_at: 创建时间
- created_by: 创建者（外键关联User）
```

**API接口**：
- `POST /api/provider/books/{id}/branches/` - 创建分支
- `GET /api/provider/books/{id}/branches/` - 获取分支列表
- `POST /api/provider/books/{id}/branches/{branch_id}/switch/` - 切换分支
- `DELETE /api/provider/books/{id}/branches/{branch_id}/` - 删除分支
- `POST /api/provider/books/{id}/branches/{branch_id}/merge/` - 合并分支
- `GET /api/provider/books/{id}/branches/{branch_id}/diff/` - 对比分支

**前端页面**：
- `BranchManagementView.vue` - 分支管理页面
- `BranchCreateModal.vue` - 分支创建弹窗
- `BranchDiffView.vue` - 分支对比页面

#### 3.3.4 版本对比
**功能描述**：对比不同版本的差异

**核心功能**：
- **书籍版本对比**
  - 对比书籍基本信息
  - 对比章节列表
  - 对比章节内容
  - 显示差异高亮

- **章节版本对比**
  - 对比章节内容
  - 对比代码内容
  - 对比多媒体资源
  - 显示差异高亮

- **对比视图**
  - 并排对比视图
  - 统一对比视图
  - 差异摘要
  - 导出对比结果

**API接口**：
- `GET /api/provider/books/{id}/versions/compare/{v1}/{v2}/` - 对比书籍版本
- `GET /api/provider/chapters/{id}/versions/compare/{v1}/{v2}/` - 对比章节版本
- `GET /api/provider/books/{id}/branches/{b1}/{b2}/diff/` - 对比分支

**前端页面**：
- `VersionCompareView.vue` - 版本对比页面
- `DiffViewer.vue` - 差异查看器组件
- `DiffSummary.vue` - 差异摘要组件

#### 3.3.5 版本回滚
**功能描述**：回滚到指定版本

**核心功能**：
- **回滚操作**
  - 回滚书籍到指定版本
  - 回滚章节到指定版本
  - 选择回滚范围（全部/部分）
  - 添加回滚说明

- **回滚确认**
  - 显示回滚影响
  - 显示回滚内容预览
  - 确认回滚操作

- **回滚历史**
  - 查看回滚记录
  - 撤销回滚操作

**API接口**：
- `POST /api/provider/books/{id}/versions/{version_id}/rollback/` - 回滚书籍
- `POST /api/provider/chapters/{id}/versions/{version_id}/rollback/` - 回滚章节
- `GET /api/provider/books/{id}/rollback-history/` - 获取回滚历史
- `POST /api/provider/books/{id}/rollback-history/{id}/undo/` - 撤销回滚

**前端页面**：
- `VersionRollbackModal.vue` - 版本回滚弹窗
- `RollbackHistoryView.vue` - 回滚历史页面
- `RollbackPreview.vue` - 回滚预览组件

### 3.4 分类与标签

#### 3.4.1 分类体系管理
**功能描述**：管理书籍分类体系

**核心功能**：
- **分类管理**
  - 创建分类
  - 编辑分类
  - 删除分类
  - 分类排序

- **分类层级**
  - 多级分类支持
  - 父子分类关系
  - 分类树展示
  - 分类拖拽排序

- **分类属性**
  - 分类名称
  - 分类描述
  - 分类图标
  - 分类颜色

**数据模型**：
```python
Category:
- id: 主键
- name: 分类名称
- description: 描述
- parent: 父分类（外键关联自身）
- icon: 图标
- color: 颜色
- order: 排序
- book_count: 书籍数量
- created_at: 创建时间
```

**API接口**：
- `POST /api/provider/categories/` - 创建分类
- `GET /api/provider/categories/` - 获取分类列表
- `GET /api/provider/categories/tree/` - 获取分类树
- `PUT /api/provider/categories/{id}/` - 更新分类
- `DELETE /api/provider/categories/{id}/` - 删除分类
- `POST /api/provider/categories/reorder/` - 分类排序

**前端页面**：
- `CategoryManagementView.vue` - 分类管理页面
- `CategoryTree.vue` - 分类树组件
- `CategoryEditor.vue` - 分类编辑器

#### 3.4.2 标签管理
**功能描述**：管理书籍标签

**核心功能**：
- **标签管理**
  - 创建标签
  - 编辑标签
  - 删除标签
  - 标签合并

- **标签属性**
  - 标签名称
  - 标签颜色
  - 标签类型
  - 标签描述

- **标签统计**
  - 标签使用次数
  - 标签关联书籍
  - 热门标签

**数据模型**：
```python
Tag:
- id: 主键
- name: 标签名称
- color: 颜色
- type: 标签类型
- description: 描述
- usage_count: 使用次数
- created_at: 创建时间
```

**API接口**：
- `POST /api/provider/tags/` - 创建标签
- `GET /api/provider/tags/` - 获取标签列表
- `PUT /api/provider/tags/{id}/` - 更新标签
- `DELETE /api/provider/tags/{id}/` - 删除标签
- `POST /api/provider/tags/merge/` - 合并标签
- `GET /api/provider/tags/popular/` - 获取热门标签

**前端页面**：
- `TagManagementView.vue` - 标签管理页面
- `TagCloud.vue` - 标签云组件
- `TagEditor.vue` - 标签编辑器

#### 3.4.3 智能推荐
**功能描述**：基于内容的智能分类和标签推荐

**核心功能**：
- **智能分类推荐**
  - 根据书籍内容推荐分类
  - 根据标题推荐分类
  - 批量分类推荐

- **智能标签推荐**
  - 根据内容推荐标签
  - 根据关键词推荐标签
  - 批量标签推荐

- **推荐应用**
  - 一键应用推荐
  - 批量应用推荐
  - 推荐置信度显示

**API接口**：
- `GET /api/provider/books/{id}/recommend-category/` - 推荐分类
- `GET /api/provider/books/{id}/recommend-tags/` - 推荐标签
- `POST /api/provider/books/{id}/apply-recommendations/` - 应用推荐
- `POST /api/provider/books/batch-recommend/` - 批量推荐

**前端页面**：
- `RecommendationPanel.vue` - 推荐面板
- `RecommendationApplyModal.vue` - 应用推荐弹窗

### 3.5 状态管理

#### 3.5.1 草稿管理
**功能描述**：管理草稿状态的书籍

**核心功能**：
- **草稿列表**
  - 查看所有草稿
  - 草稿排序
  - 草稿搜索
  - 草稿筛选

- **草稿操作**
  - 继续编辑
  - 提交审核
  - 删除草稿
  - 草稿复制

- **自动保存**
  - 自动保存草稿
  - 保存历史记录
  - 恢复历史版本

**API接口**：
- `GET /api/provider/books/drafts/` - 获取草稿列表
- `POST /api/provider/books/{id}/submit-review/` - 提交审核
- `GET /api/provider/books/{id}/draft-history/` - 获取草稿历史
- `POST /api/provider/books/{id}/restore-draft/` - 恢复草稿

**前端页面**：
- `DraftManagementView.vue` - 草稿管理页面
- `DraftAutoSaveIndicator.vue` - 自动保存指示器

#### 3.5.2 审核流程
**功能描述**：管理书籍审核流程

**核心功能**：
- **审核提交**
  - 提交审核申请
  - 填写审核说明
  - 选择审核人员
  - 设置审核截止时间

- **审核管理**
  - 查看待审核书籍
  - 审核书籍
  - 审核意见
  - 审核历史

- **审核状态**
  - 待审核
  - 审核中
  - 审核通过
  - 审核驳回
  - 需要修改

- **审核反馈**
  - 添加审核意见
  - 标注问题位置
  - 提供修改建议
  - 通知作者

**数据模型**：
```python
Review:
- id: 主键
- book: 书籍（外键关联Book）
- reviewer: 审核人员（外键关联User）
- status: 审核状态（pending/reviewing/approved/rejected/needs_revision）
- comments: 审核意见
- issues: 问题列表（JSON）
- submitted_at: 提交时间
- reviewed_at: 审核时间
- due_date: 截止时间
```

**API接口**：
- `POST /api/provider/books/{id}/submit-review/` - 提交审核
- `GET /api/provider/reviews/pending/` - 获取待审核列表
- `GET /api/provider/reviews/my/` - 获取我的审核任务
- `POST /api/provider/reviews/{id}/approve/` - 审核通过
- `POST /api/provider/reviews/{id}/reject/` - 审核驳回
- `POST /api/provider/reviews/{id}/request-revision/` - 要求修改
- `GET /api/provider/books/{id}/review-history/` - 获取审核历史

**前端页面**：
- `ReviewSubmitModal.vue` - 审核提交弹窗
- `ReviewListView.vue` - 审核列表页面
- `ReviewDetailView.vue` - 审核详情页面
- `ReviewCommentEditor.vue` - 审核意见编辑器

#### 3.5.3 发布管理
**功能描述**：管理书籍发布

**核心功能**：
- **发布操作**
  - 发布书籍
  - 设置发布时间（立即/定时）
  - 发布说明
  - 发布通知

- **发布版本**
  - 选择发布版本
  - 版本说明
  - 变更日志

- **发布后管理**
  - 查看已发布书籍
  - 撤销发布
  - 更新发布内容
  - 发布统计

**API接口**：
- `POST /api/provider/books/{id}/publish/` - 发布书籍
- `POST /api/provider/books/{id}/schedule-publish/` - 定时发布
- `POST /api/provider/books/{id}/unpublish/` - 撤销发布
- `GET /api/provider/books/published/` - 获取已发布书籍
- `POST /api/provider/books/{id}/update-published/` - 更新发布内容

**前端页面**：
- `PublishModal.vue` - 发布弹窗
- `SchedulePublishModal.vue` - 定时发布弹窗
- `PublishedBooksView.vue` - 已发布书籍页面

#### 3.5.4 下架管理
**功能描述**：管理书籍下架

**核心功能**：
- **下架操作**
  - 下架书籍
  - 下架原因
  - 下架说明
  - 下架通知

- **下架管理**
  - 查看已下架书籍
  - 恢复上架
  - 永久删除
  - 下架统计

**API接口**：
- `POST /api/provider/books/{id}/unpublish/` - 下架书籍
- `GET /api/provider/books/unpublished/` - 获取已下架书籍
- `POST /api/provider/books/{id}/republish/` - 恢复上架
- `GET /api/provider/books/unpublish-stats/` - 获取下架统计

**前端页面**：
- `UnpublishModal.vue` - 下架弹窗
- `UnpublishedBooksView.vue` - 已下架书籍页面

### 3.6 资源管理

#### 3.6.1 文件上传
**功能描述**：上传和管理各种文件资源

**核心功能**：
- **文件上传**
  - 支持多种文件格式
  - 批量上传
  - 拖拽上传
  - 断点续传

- **上传设置**
  - 文件大小限制
  - 文件格式限制
  - 自动重命名
  - 自动压缩

- **上传进度**
  - 实时进度显示
  - 上传速度显示
  - 剩余时间估算
  - 上传队列管理

**API接口**：
- `POST /api/provider/resources/upload/` - 上传文件
- `POST /api/provider/resources/batch-upload/` - 批量上传
- `GET /api/provider/resources/upload-status/{id}/` - 获取上传状态
- `POST /api/provider/resources/upload-cancel/{id}/` - 取消上传

**前端组件**：
- `FileUploader.vue` - 文件上传器
- `BatchUploader.vue` - 批量上传器
- `UploadProgress.vue` - 上传进度组件

#### 3.6.2 资源库
**功能描述**：集中管理所有资源

**核心功能**：
- **资源浏览**
  - 按类型浏览
  - 按书籍浏览
  - 按时间浏览
  - 按大小浏览

- **资源搜索**
  - 按名称搜索
  - 按类型搜索
  - 按标签搜索
  - 高级搜索

- **资源管理**
  - 重命名资源
  - 移动资源
  - 复制资源
  - 删除资源

- **资源统计**
  - 存储空间统计
  - 资源类型统计
  - 使用频率统计

**API接口**：
- `GET /api/provider/resources/` - 获取资源列表
- `GET /api/provider/resources/search/` - 搜索资源
- `PUT /api/provider/resources/{id}/` - 更新资源
- `DELETE /api/provider/resources/{id}/` - 删除资源
- `GET /api/provider/resources/stats/` - 获取资源统计

**前端页面**：
- `ResourceLibraryView.vue` - 资源库页面
- `ResourceGrid.vue` - 资源网格组件
- `ResourceStatsPanel.vue` - 资源统计面板

#### 3.6.3 资源预览
**功能描述**：在线预览各种资源

**核心功能**：
- **图片预览**
  - 缩放查看
  - 旋转查看
  - 全屏查看
  - 幻灯片模式

- **视频预览**
  - 在线播放
  - 进度控制
  - 倍速播放
  - 全屏播放

- **文档预览**
  - PDF预览
  - Word预览
  - Excel预览
  - PPT预览

- **代码预览**
  - 语法高亮
  - 代码折叠
  - 行号显示
  - 复制代码

**API接口**：
- `GET /api/provider/resources/{id}/preview/` - 预览资源
- `GET /api/provider/resources/{id}/preview-info/` - 获取预览信息

**前端组件**：
- `ImagePreview.vue` - 图片预览组件
- `VideoPreview.vue` - 视频预览组件
- `DocumentPreview.vue` - 文档预览组件
- `CodePreview.vue` - 代码预览组件

#### 3.6.4 资源下载
**功能描述**：下载资源

**核心功能**：
- **单个下载**
  - 下载单个资源
  - 选择下载格式
  - 下载进度显示

- **批量下载**
  - 批量选择资源
  - 打包下载
  - 下载进度显示

- **下载历史**
  - 查看下载历史
  - 重新下载
  - 下载统计

**API接口**：
- `GET /api/provider/resources/{id}/download/` - 下载资源
- `POST /api/provider/resources/batch-download/` - 批量下载
- `GET /api/provider/resources/download-history/` - 获取下载历史

**前端组件**：
- `ResourceDownloadButton.vue` - 下载按钮组件
- `BatchDownloadModal.vue` - 批量下载弹窗
- `DownloadHistoryView.vue` - 下载历史页面

### 3.7 协作管理

#### 3.7.1 多人协作
**功能描述**：支持多人同时编辑

**核心功能**：
- **协作邀请**
  - 邀请协作者
  - 设置协作权限
  - 协作者管理
  - 协作历史

- **实时协作**
  - 实时编辑
  - 实时光标显示
  - 实时更新
  - 冲突解决

- **协作权限**
  - 查看权限
  - 编辑权限
  - 评论权限
  - 管理权限

**数据模型**：
```python
Collaborator:
- id: 主键
- book: 书籍（外键关联Book）
- user: 用户（外键关联User）
- role: 角色（owner/editor/viewer）
- permissions: 权限（JSON）
- invited_at: 邀请时间
- invited_by: 邀请人（外键关联User）
```

**API接口**：
- `POST /api/provider/books/{id}/collaborators/` - 邀请协作者
- `GET /api/provider/books/{id}/collaborators/` - 获取协作者列表
- `PUT /api/provider/books/{id}/collaborators/{user_id}/` - 更新协作者权限
- `DELETE /api/provider/books/{id}/collaborators/{user_id}/` - 移除协作者
- `GET /api/provider/books/{id}/collaboration-history/` - 获取协作历史

**前端页面**：
- `CollaboratorManagementView.vue` - 协作者管理页面
- `CollaboratorInviteModal.vue` - 邀请协作者弹窗
- `RealTimeEditor.vue` - 实时编辑器组件

#### 3.7.2 评论与反馈
**功能描述**：添加和管理评论

**核心功能**：
- **评论功能**
  - 添加评论
  - 回复评论
  - 编辑评论
  - 删除评论

- **评论位置**
  - 全局评论
  - 章节评论
  - 段落评论
  - 代码评论

- **评论通知**
  - 评论通知
  - 回复通知
  - 提及通知
  - 通知管理

**数据模型**：
```python
Comment:
- id: 主键
- book: 书籍（外键关联Book）
- chapter: 章节（外键关联Chapter, 可为空）
- user: 用户（外键关联User）
- parent: 父评论（外键关联自身, 可为空）
- content: 评论内容
- position: 位置信息（JSON）
- created_at: 创建时间
- updated_at: 更新时间
```

**API接口**：
- `POST /api/provider/books/{id}/comments/` - 添加评论
- `GET /api/provider/books/{id}/comments/` - 获取评论列表
- `PUT /api/provider/comments/{id}/` - 更新评论
- `DELETE /api/provider/comments/{id}/` - 删除评论
- `POST /api/provider/comments/{id}/reply/` - 回复评论

**前端组件**：
- `CommentPanel.vue` - 评论面板
- `CommentEditor.vue` - 评论编辑器
- `CommentItem.vue` - 评论项组件

#### 3.7.3 审核流程
**功能描述**：管理审核流程

**核心功能**：
- **审核流程设置**
  - 设置审核流程
  - 设置审核节点
  - 设置审核人员
  - 设置审核规则

- **审核流程执行**
  - 提交审核
  - 审核流转
  - 审核记录
  - 审核统计

- **审核流程优化**
  - 流程优化建议
  - 效率分析
  - 问题识别

**数据模型**：
```python
ReviewWorkflow:
- id: 主键
- book: 书籍（外键关联Book）
- name: 流程名称
- steps: 审核步骤（JSON）
- current_step: 当前步骤
- status: 状态
- created_at: 创建时间
```

**API接口**：
- `POST /api/provider/books/{id}/review-workflows/` - 创建审核流程
- `GET /api/provider/books/{id}/review-workflows/` - 获取审核流程
- `PUT /api/provider/review-workflows/{id}/` - 更新审核流程
- `POST /api/provider/review-workflows/{id}/next-step/` - 下一步
- `GET /api/provider/review-workflows/{id}/history/` - 获取审核历史

**前端页面**：
- `ReviewWorkflowView.vue` - 审核流程页面
- `WorkflowDesigner.vue` - 流程设计器
- `WorkflowHistory.vue` - 流程历史

#### 3.7.4 权限管理
**功能描述**：管理用户权限

**核心功能**：
- **角色管理**
  - 创建角色
  - 编辑角色
  - 删除角色
  - 角色权限设置

- **权限设置**
  - 查看权限
  - 编辑权限
  - 删除权限
  - 发布权限

- **用户权限**
  - 查看用户权限
  - 修改用户权限
  - 批量设置权限

**数据模型**：
```python
Role:
- id: 主键
- name: 角色名称
- description: 描述
- permissions: 权限列表（JSON）
- created_at: 创建时间
```

**API接口**：
- `POST /api/provider/roles/` - 创建角色
- `GET /api/provider/roles/` - 获取角色列表
- `PUT /api/provider/roles/{id}/` - 更新角色
- `DELETE /api/provider/roles/{id}/` - 删除角色
- `GET /api/provider/users/{id}/permissions/` - 获取用户权限
- `PUT /api/provider/users/{id}/permissions/` - 更新用户权限

**前端页面**：
- `RoleManagementView.vue` - 角色管理页面
- `PermissionEditor.vue` - 权限编辑器
- `UserPermissionView.vue` - 用户权限页面

### 3.8 数据分析

#### 3.8.1 使用统计
**功能描述**：统计教材使用情况

**核心功能**：
- **学习统计**
  - 学习人数统计
  - 学习时长统计
  - 学习进度统计
  - 学习完成率统计

- **访问统计**
  - 访问次数统计
  - 访问用户统计
  - 访问时间分布
  - 访问来源统计

- **下载统计**
  - 下载次数统计
  - 下载用户统计
  - 下载时间分布
  - 下载格式统计

- **分享统计**
  - 分享次数统计
  - 分享平台统计
  - 分享效果统计

**数据可视化**：
- 学习人数趋势图
- 学习时长分布图
- 访问热力图
- 下载统计图

**API接口**：
- `GET /api/provider/books/{id}/stats/learning/` - 获取学习统计
- `GET /api/provider/books/{id}/stats/access/` - 获取访问统计
- `GET /api/provider/books/{id}/stats/download/` - 获取下载统计
- `GET /api/provider/books/{id}/stats/share/` - 获取分享统计

**前端页面**：
- `UsageStatsView.vue` - 使用统计页面
- `LearningStatsChart.vue` - 学习统计图表
- `AccessHeatmap.vue` - 访问热力图

#### 3.8.2 内容分析
**功能描述**：分析教材内容质量

**核心功能**：
- **内容质量分析**
  - 章节完成度分析
  - 内容丰富度分析
  - 多媒体使用分析
  - 代码质量分析

- **学习效果分析**
  - 知识点掌握度
  - 练习题正确率
  - 学习反馈分析
  - 评价分析

- **内容优化建议**
  - 内容改进建议
  - 章节调整建议
  - 练习题优化建议
  - 资源优化建议

**API接口**：
- `GET /api/provider/books/{id}/analytics/quality/` - 获取内容质量分析
- `GET /api/provider/books/{id}/analytics/effectiveness/` - 获取学习效果分析
- `GET /api/provider/books/{id}/analytics/suggestions/` - 获取优化建议

**前端页面**：
- `ContentAnalyticsView.vue` - 内容分析页面
- `QualityScoreChart.vue` - 质量评分图表
- `OptimizationSuggestions.vue` - 优化建议组件

#### 3.8.3 效果评估
**功能描述**：评估教材教学效果

**核心功能**：
- **教学效果评估**
  - 学习成果评估
  - 知识掌握度评估
  - 能力提升评估
  - 满意度评估

- **对比分析**
  - 与其他教材对比
  - 与历史数据对比
  - 与行业标准对比

- **效果报告**
  - 生成效果报告
  - 导出报告
  - 分享报告

**API接口**：
- `GET /api/provider/books/{id}/evaluation/teaching/` - 获取教学效果评估
- `GET /api/provider/books/{id}/evaluation/comparison/` - 获取对比分析
- `POST /api/provider/books/{id}/evaluation/report/` - 生成效果报告

**前端页面**：
- `EffectEvaluationView.vue` - 效果评估页面
- `ComparisonChart.vue` - 对比图表
- `EvaluationReport.vue` - 效果报告组件

## 4. 系统架构设计

### 4.1 技术架构
```
前端架构
├── Vue 3.x
├── Vue Router
├── Pinia（状态管理）
├── Element Plus（UI组件库）
├── Monaco Editor（代码编辑器）
├── Quill.js（富文本编辑器）
└── ECharts（数据可视化）

后端架构
├── Django REST Framework
├── Django Channels（WebSocket）
├── Celery（异步任务）
├── Redis（缓存）
├── PostgreSQL（数据库）
└── MinIO（对象存储）
```

### 4.2 数据库设计
核心数据表：
- `books` - 书籍表
- `chapters` - 章节表
- `book_versions` - 书籍版本表
- `chapter_versions` - 章节版本表
- `book_branches` - 书籍分支表
- `categories` - 分类表
- `tags` - 标签表
- `media_resources` - 多媒体资源表
- `reviews` - 审核表
- `comments` - 评论表
- `collaborators` - 协作者表
- `roles` - 角色表
- `permissions` - 权限表

### 4.3 API设计原则
- RESTful API设计
- 统一的响应格式
- 完善的错误处理
- 接口版本控制
- 请求限流
- 数据验证

## 5. 安全设计

### 5.1 认证与授权
- JWT Token认证
- 基于角色的访问控制（RBAC）
- 权限细粒度控制
- 操作日志记录

### 5.2 数据安全
- 数据加密存储
- 传输加密（HTTPS）
- 敏感信息脱敏
- 数据备份

### 5.3 内容安全
- 内容审核
- 敏感词过滤
- 文件类型检查
- 文件大小限制

## 6. 性能优化

### 6.1 前端优化
- 代码分割
- 懒加载
- 图片优化
- 缓存策略

### 6.2 后端优化
- 数据库索引优化
- 查询优化
- 缓存策略
- 异步处理

### 6.3 存储优化
- CDN加速
- 对象存储
- 图片压缩
- 视频转码

## 7. 扩展功能

### 7.1 AI辅助功能
- AI智能写作辅助
- AI内容优化建议
- AI自动生成练习题
- AI智能审核

### 7.2 多语言支持
- 多语言界面
- 多语言内容
- 自动翻译
- 语言切换

### 7.3 移动端适配
- 响应式设计
- 移动端优化
- 离线支持
- 推送通知

## 8. 开发计划

### 8.1 开发阶段
- **第一阶段**（P0功能）：书籍管理、内容编辑、版本管理
- **第二阶段**（P1功能）：分类与标签、状态管理、资源管理
- **第三阶段**（P2功能）：协作管理、数据分析、AI辅助

### 8.2 测试计划
- 单元测试
- 集成测试
- 性能测试
- 安全测试
- 用户验收测试

### 8.3 上线计划
- 灰度发布
- 数据迁移
- 监控告警
- 用户培训

---

**文档版本**：v1.0  
**创建日期**：2026-01-05  
**最后更新**：2026-01-05
