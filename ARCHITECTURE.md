# CodeBook+ 多端架构说明

## 概述

CodeBook+ 平台采用多端架构设计，支持三种不同的用户角色，每个角色都有独立的功能模块和界面。

## 系统架构

```
CodeBook+ 平台
├── 学生端（Student）
│   ├── 学习中心
│   ├── 教材学习
│   ├── 练习与实战
│   ├── AI智能辅助
│   ├── 学习记录
│   └── 个人中心
│
├── 教师端（Teacher）
│   ├── 班级管理
│   ├── 教学管理
│   ├── 作业管理
│   ├── 学生管理
│   └── 数据分析
│
└── 教材提供者端（Provider）
    ├── 工具箱
    ├── 书籍管理
    ├── 内容编辑
    ├── 版本管理
    ├── 分类与标签
    └── 状态管理
```

## 前端架构

### 目录结构
```
front1.0/src/
├── student/          # 学生端模块
│   ├── api/         # 学生端API接口
│   ├── components/  # 学生端组件
│   ├── views/       # 学生端页面
│   └── router/      # 学生端路由
│
├── teacher/         # 教师端模块
│   ├── api/         # 教师端API接口
│   ├── components/  # 教师端组件
│   ├── views/       # 教师端页面
│   └── router/      # 教师端路由
│
├── provider/        # 教材提供者端模块
│   ├── api/         # 教材提供者端API接口
│   ├── components/  # 教材提供者端组件
│   ├── views/       # 教材提供者端页面
│   └── router/      # 教材提供者端路由
│
└── router/          # 主路由（整合所有端）
```

### 路由前缀
- 学生端：`/student/*`
- 教师端：`/teacher/*`
- 教材提供者端：`/provider/*`

### 默认入口
访问 `/` 会自动重定向到 `/student/books`

## 后端架构

### API路径结构
```
/api/
├── student/         # 学生端API
│   ├── books/      # 书籍浏览
│   ├── learning/   # 学习记录、练习等
│   └── users/      # 用户相关
│
├── teacher/         # 教师端API
│   ├── classes/    # 班级管理
│   ├── assignments/# 作业管理
│   ├── students/   # 学生管理
│   └── analytics/  # 数据分析
│
└── provider/        # 教材提供者端API
    ├── books/      # 书籍管理
    └── toolkit/    # 工具箱管理
```

### 应用结构
```
back1.0/apps/
├── users/          # 用户管理（共用）
├── books/          # 书籍管理
├── learning/       # 学习相关（学生端）
├── teacher/        # 教师端功能
└── toolkit/        # 工具箱（教材提供者端）
```

## 数据库

- **类型**：MySQL
- **数据库名**：`codebook`
- **字符集**：utf8mb4

## 开发指南

### 前端开发
1. 在对应端的目录下创建组件和页面
2. 在对应端的router中添加路由配置
3. 使用对应端的API接口

### 后端开发
1. 在对应的app下创建视图和序列化器
2. 在urls.py中使用正确的路径前缀
3. 实现权限控制

### 权限控制
- 前端：路由守卫检查用户角色
- 后端：视图级别权限检查

## 迁移说明

### 已完成的迁移
1. ✅ 创建多端目录结构
2. ✅ 将学生端功能整理到student目录
3. ✅ 更新路由配置
4. ✅ 更新API路径
5. ✅ 创建教师端和教材提供者端基础结构

### 后续开发
- 教师端功能开发
- 教材提供者端功能开发
- 权限系统完善
- API文档更新

## 注意事项

1. 所有学生端相关的代码都在 `front1.0/src/student/` 目录下
2. 旧版API路径仍然可用，但建议使用新的路径前缀
3. 路由配置中包含角色检查，确保用户只能访问对应端的页面
4. 数据库已从SQLite迁移到MySQL

